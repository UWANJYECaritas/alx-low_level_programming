#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase.
 *
 * Return: Always 0 (success)
 */
void print_alphabet(void)
{
	int i;

	for  (i = 'a'; i <= 'b'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
